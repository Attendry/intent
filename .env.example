# Gemini AI API key (can also be set in the Settings page)
GEMINI_API_KEY=

# Base URL for internal API calls (used by server-side fetches)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Database URL (defaults to file:./dev.db if not set)
DATABASE_URL="file:./dev.db"
